#ArgoCD application for the dev environment.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: qba-dev #This becomes the release name.
  namespace: argocd
spec:
  project: home-lab
  source:
    repoURL: git@github.com:Ranuldeepanayake/helm.git
    targetRevision: develop
    path: charts/qba
    helm:
      valueFiles:
        - ../../environments/qba/dev/values.yaml
        - ../../environments/qba/dev/qba-back-end-env.yaml
        #- ../../environments/qba/dev/qba-back-end-secrets.yaml
        - ../../environments/qba/dev/qba-rabbitmq-env.yaml
        #- ../../environments/qba/dev/qba-rabbitmq-secrets.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: qba-dev
  syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
      syncOptions:
        - CreateNamespace=true
        - ApplyOutOfSyncOnly=false
        - Validate=false
        - Replace=false
        - Force=false
        - ServerSideApply=false