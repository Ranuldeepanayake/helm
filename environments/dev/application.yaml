#ArgoCD application for the dev environment.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: qba-dev #This becomes the release name.
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:Ranuldeepanayake/qba-helm.git
    targetRevision: develop
    path: charts/qba
    helm:
      valueFiles:
        - ../../environments/dev/values.yaml
        - ../../environments/dev/qba-back-end-values-env.yaml
        #- ../../environments/dev/qba-back-end-values-secrets.yaml
        - ../../environments/dev/qba-rabbitmq-values-env.yaml
        #- ../../environments/dev/qba-rabbitmq-values-secrets.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: qba-dev
  syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
      syncOptions:
        - CreateNamespace=true
        - ApplyOutOfSyncOnly=false
        - Validate=false
        - Replace=false
        - Force=false
        - ServerSideApply=false